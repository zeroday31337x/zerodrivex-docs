/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // App Router is default in Next 16; no flags needed
  experimental: {
    // Keep empty unless you explicitly need something later
  },
};

module.exports = nextConfig;
